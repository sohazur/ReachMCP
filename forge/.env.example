# ── Firebase Configuration (NO service account needed!) ────────
# Required: Firebase Project ID
# Get from: Firebase Console → Project Settings → General → Project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# Required: Firebase Web API Key
# Get from: Firebase Console → Project Settings → General → Web API Key
FIREBASE_API_KEY=your-firebase-api-key

# ── MCP Server ──────────────────────────────────────────────────
# Server URL (auto-set in production)
MCP_URL=http://localhost:3000

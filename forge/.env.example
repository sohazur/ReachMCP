# ── Firebase Configuration ──────────────────────────────────────
# Required: Firebase Project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# Required: Firebase Web API Key (from Firebase Console → Project Settings → General)
FIREBASE_API_KEY=your-firebase-api-key

# Optional: Firebase Service Account JSON (for admin operations)
# Paste the entire JSON content of your service account key file
# Get from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
FIREBASE_SERVICE_ACCOUNT_JSON=

# ── MCP Server ──────────────────────────────────────────────────
# Server URL (auto-set in production)
MCP_URL=http://localhost:3000
